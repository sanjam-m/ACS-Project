name: terraform-lint
on:
  push:
    branches: [ ]
  pull_request:
    branches: [ ]  

jobs:
  delivery:
    runs-on: ubuntu-latest
    steps:
    - name: Check out code
      uses: actions/checkout@main
    - name: Lint Terraform
      uses: actionshub/terraform-lint@main
